/*
 * pccore.h -- HOOTRIP stub
 *
 * The i286c core #includes <pccore.h> but, with SUPPORT_ASYNC_CPU undefined,
 * references none of its contents (the only pccore.* uses live in a fully
 * commented-out async-CPU block in i286c.c). Intentionally empty.
 */
#ifndef HOOTRIP_PCCORE_H
#define HOOTRIP_PCCORE_H

#include <np2types.h>

#endif /* HOOTRIP_PCCORE_H */

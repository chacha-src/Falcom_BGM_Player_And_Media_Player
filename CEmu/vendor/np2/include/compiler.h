/*
 * compiler.h -- HOOTRIP stub
 *
 * The vendored i286c core does `#include <compiler.h>` as its first line.
 * Upstream this pulls in NP2's full SDL/glib/Windows base layer; here we
 * redirect it to the minimal self-contained np2types.h glue header.
 */
#ifndef HOOTRIP_COMPILER_H
#define HOOTRIP_COMPILER_H

#include <np2types.h>

#endif /* HOOTRIP_COMPILER_H */
